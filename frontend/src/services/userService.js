// User service placeholder
const userService = {
  getUser: async (id) => {
    // Fetch user by id
  },
  updateUser: async (id, data) => {
    // Update user info
  },
};

export default userService;
